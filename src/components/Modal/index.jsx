import React from 'react'
import { isModalOpen } from '../../redux/modal/selectors'

export default function Modal({title, content, onSubmit, modalMode}) {




  return (
    <div>
        <h1>title</h1>
    </div>
  )
}
