export const selectIsModalOpen = (state) => state.modal.isModalOpen;
export const selectTransactionId = (state) => state.modal.transactionId;
export const selectModalMode = (state) => state.modal.modalMode;